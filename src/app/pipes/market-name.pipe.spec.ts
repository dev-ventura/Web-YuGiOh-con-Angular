import { MarketNamePipe } from './market-name.pipe';

describe('MarketNamePipe', () => {
  it('create an instance', () => {
    const pipe = new MarketNamePipe();
    expect(pipe).toBeTruthy();
  });
});
